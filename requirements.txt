# Project Orion — Python dependencies
# Install: pip install -r requirements.txt
#
# These are the direct dependencies used by library_architect, edge_finder,
# and proof_oracle. Transitive dependencies are resolved by pip automatically.

# LLM client (used by edge_finder, library_architect via edge_finder)
openai>=1.0

# HTTP client (used by library_architect grounder, edge_finder, proof_oracle)
httpx>=0.25

# Environment variable loading (used by library_architect)
python-dotenv>=1.0

# LeanExplore Python SDK (used by edge_finder, proof_oracle)
lean-explore>=1.2
